services:
  # React Frontend only
  ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vostok-ui
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    environment:
      - NGINX_HOST=0.0.0.0
      - NGINX_PORT=80
